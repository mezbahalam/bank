<% if customer_signed_in? %>
    <%= link_to("Sign out", destroy_customer_session_path, method: :delete) %>
    <h1>Name: <%= current_customer.name %></h1>
    <h2>Your balance is $<%= current_customer.balance %></h2>
<% end %>